# Agent Advantage Report

The three required task records are not claimed until the corresponding Agent Studio agents have real onchain runs. Add raw logs, transaction hashes, time, cost, and output-quality comparisons under this directory.
