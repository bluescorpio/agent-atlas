/**
 * ABI intentionally empty until the official BSC ERC-8004 deployment is
 * confirmed. Live mode fails closed rather than guessing function signatures.
 */
export const identityRegistryAbi = [] as const;
export const reputationRegistryAbi = [] as const;
