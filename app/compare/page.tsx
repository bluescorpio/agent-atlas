import Link from 'next/link';
import { getMockAgents } from '../../lib/chain/mock';
export default function ComparePage(){ const agents=getMockAgents().slice(0,4); return <main className="route-page"><header className="route-head"><Link href="/">← Agent Atlas</Link><span>COMPARE · DEMO DATA</span></header><div className="route-content"><div className="eyebrow muted">SIDE BY SIDE</div><h1>Compare agents.</h1><p>Compare agents within one category before you activate.</p><div className="compare-table">{agents.map(a=><div className="compare-col" key={a.identity.agentId}><h2>{a.identity.name}</h2><span>{a.category}</span><strong>{a.pricing.amount} {a.pricing.token}</strong><p>{a.identity.description}</p><Link className="ghost" href={`/a/${a.identity.agentId}`}>Open details ↗</Link></div>)}</div></div></main> }
